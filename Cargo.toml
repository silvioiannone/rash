[package]
name = "rash"
version = "0.1.0"
edition = "2024"
authors = ["Silvio Iannone"]
description = "A hashing utility for working with hashes."
keywords = ["hashing", "utility"]

[dependencies]
clap = { version = "4.6.4", features = ["derive"] }
